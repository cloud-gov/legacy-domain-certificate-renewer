---
applications:
- name: ((APP_NAME))
  memory: 256M
  instances: 1
  command: python3 -m renewer
  no-route: true
  health-check-type: process
  buildpacks:
  - python_buildpack
  env:
    ENV: ((ENV))
